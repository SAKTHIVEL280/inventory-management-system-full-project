[pytest]
# Add the backend/ directory (where this file lives = rootdir) to sys.path so the
# `app` package is importable when tests are run via the `pytest` console script
# (which, unlike `python -m pytest`, does NOT add the current directory to the path).
pythonpath = .
testpaths = tests
