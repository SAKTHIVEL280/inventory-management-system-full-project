# Frontend Updates Log — BRD Bugfix Sprint

**Started**: June 9, 2026
**Reference**: docs/requirements/BRD_Bugfix.md (MCN-BUG-001 → MCN-BUG-004)

---

- **FE-201** (MCN-BUG-001): Invoices page now server-paginated — rows-per-page (20/50/100/All), Prev/Next, page indicator; search & date filters moved server-side.
- **FE-202** (MCN-BUG-003): Record Payment requires ≥1 invoice allocation — inline error, disabled Save; list flags legacy unallocated receipts with a red badge.
- **FE-203** (MCN-BUG-004): GRN — Reverse button (confirmed, admin) with mandatory-reason modal; detail view shows read-only Created Date/By + reversal info; 'reversed' status filter/badge.
- **FE-204** (MCN-BUG-002): Dashboard — removed Recent Invoices widget; added side-by-side Sales Manager & Stockist revenue bar charts with independent Day/Week/Month toggles, distinct colors, tooltips.

### BRD Bugfix v1.5 (docs/requirements/BRD_Bugfix_v1.5.md)

- **FE-205** (MCN-BUG-004-ii): GRN detail modal — "Edit" button (confirmed GRNs, `grn_write` users) unlocks in-place editing of Batch/Qty/Free/Rate/MFG/EXP per line + Remarks, with Save/Cancel and client-side validation. Save calls `PUT /grn/{id}/confirmed-details`; on success re-fetches GRN + refreshes lists. GRN number/supplier/created details stay read-only. New `purchaseApi.updateConfirmedGRN` + `ConfirmedGRNEditPayload` types.
- **FE-206** (MCN-BUG-005): Reports → Sales Report — added "Customer Name" column; added a Customer Name filter input, a sortable Customer Name header (asc/desc/none), and an "Export CSV" button (BOM-prefixed, Excel-compatible) including Customer Name.
- **FE-207** (MCN-BUG-006): Action Logs — added a sortable "Version" column rendering the Roman-numeral `version_label` (e.g. "Version I") for Sales Invoice edits; `ActionLogItem` gains `version`/`version_label`.
- **FE-208** (Company profile logos): Company profile now renders the Company Logo and Ambassador Logo via the backend-served `/api/v1/company/(logo|ambassador-logo)-file` endpoints (cache-busted by stored filename) instead of the raw `/static/*` path that 404s on the deployed origin — logos now display instead of the placeholder.
- **FE-209** (Human-readable audit descriptions): Action Logs description cell now uses `whitespace-pre-line` so the backend's multi-line business descriptions render as separate lines (headline + one line per field change), e.g. "Invoice INV-00027 updated." / "Amount changed from INR 6,500.00 to INR 7,080.00." No data-shape change — descriptions are composed server-side (see BE-209).

### BRD Bugfix — GRN Batch / Invoice Edit-After-Issue / Country Master

- **FE-210** (GRN — Batch mandatory): GRN create/edit form (`GRNPage`) now blocks save when any line is missing a batch number ("Line item N: Batch number is required.") in `handleSubmit`, and the confirmed-GRN edit modal blocks save ("Batch number is required for every line item.") — mirroring the server rules for instant feedback. The "Batch No" column header in the create form is marked required (`*`).
- **FE-211** (Sales Invoice — Edit after issue): Invoices page (`InvoicesPage`) detail view shows an "Edit Issued Invoice" button for admins when the invoice is `issued` with no recorded payment (`amount_paid === 0`), alongside the existing draft "Edit Invoice" button. Both reuse a shared `loadInvoiceIntoEditForm(issued)` loader; an `editingIssued` flag routes the save to the new `salesApi.updateIssuedInvoice` (`PUT /api/v2/invoices/{id}/issued-details`) so stock, tax and totals are recalculated server-side, while draft edits keep using `updateInvoice`. New `salesApi.updateIssuedInvoice` client method.
- **FE-212** (Customer & Supplier — Country master): New shared `src/constants/countries.ts` `COUNTRY_MASTER` (complete country list). `CustomersPage` and `SuppliersPage` now seed their country typeahead from this master list (replacing the local 6-item `DEFAULT_COUNTRIES`), merged with the customization-options API values. The existing searchable `TypeaheadInput` is retained (free-text allowed, so existing/legacy country values still display and save). Applied to billing and shipping country fields consistently in both modules.
- **FE-213** (Clear error messages — array `detail` handling): **Root-cause fix** for the generic "Invalid input. Please check the entered values." Backends raise some business-rule errors as a `detail` that is an **array of plain strings** (e.g. `["Supplier 'X' has no State set…"]` from GRN GST validation). The error formatters only handled arrays of *objects* (`d.msg`/`d.message`), so a string array produced no message and fell through to the generic 400 fallback — hiding the real reason from the user. Fixed in every place that renders `detail`: `normalizeApiErrorMessage` (`api/client.ts`, global toast), `getApiDetailMessage` (`utils/apiError.ts`), the GRN create/edit catch (`GRNPage.tsx`), and the inline handlers in `CustomersPage`/`SuppliersPage`/`ProductsPage` — each now renders `typeof d === 'string' ? d : d.msg || d.message`. The `api/client.ts` array path additionally prefixes object-style (422) messages with the field name from `loc` (e.g. "items #1 quantity: must be a valid number"). Types widened: `ApiErrorDetail` (client) and `ApiDetail` (apiError) array elements are now `string | object`. Together with the backend BE-213 validation handler, users now see the exact reason/field that failed.
